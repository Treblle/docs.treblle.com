---
title: Treblle with Strapi
description: How to add Treblle to a Strapi API.
---

import { Schema } from "astro-seo-schema"

<Schema
  item={{
    "@context": "https://schema.org",
    "@type": "VideoObject",
    name: "Setting up API monitoring and observability for Strapi Content API",
    description: "Easily setup API monitoring and observability for your Strapi Content API with the Treblle plugin for Strapi.  🔗 LINKS   Docs https://docs.treblle.com/en/integrations/strapi  Strapi plugin for Treblle https://market.strapi.io/plugins/@treblle-strapi   📞 CONNECT: 🌍 Website - https://treblle.com 😺 GitHub - https://github.com/Treblle 🐦 Twitter - https://twitter.com/treblleapi 👍🏽 Facebook - https://facebook.com/treblle 📪 Email - info@treblle.com  #treblle #projects #API #development #strapi",
    thumbnailUrl: "https://i.ytimg.com/vi/-QvnDNk90pE/default.jpg",
    uploadDate: "2023-07-12T12:51:24Z",
    duration: "PT3M29S",
    embedUrl: "https://www.youtube.com/embed/-QvnDNk90pE",
    interactionCount: "202"
  }}
/>

<iframe
  class="w-full aspect-[16/9]"
  src="https://www.youtube.com/embed/-QvnDNk90pE"
  aria-label="YouTube video player"
  frameborder="0"
  allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
  allowfullscreen
></iframe>

To integrate Treblle with [Strapi](http://strapi.io), we provide an official SDK for Strapi which is a Strapi plugin - [`treblle-strapi`](https://github.com/Treblle/treblle-strapi/).

## Requirements

- Strapi v4

## Installation

```sh
npm i @treblle/strapi --save
```

After installation set the following environment variables your `.env`

> You can find the values in your Treblle dashboard

- `TREBLLE_API_KEY`
- `TREBLLE_PROJECT_ID`

## Setup

`treblle-strapi` exposes a middleware that you can add in your `config/middlewares.js` array like so:

```js
module.exports = [
  //...
  'plugin::treblle.treblle',
]
```

And that's it, Treblle will now monitor your Strapi content API.

## Config

### AdditionalFieldsToMask

You can specify additional fields to be [masked](/security/masking/) before sending the Treblle payload. To do so, add the following config in `config/plugins.js`

```js
module.exports = {
  treblle: {
    config: {
      additionalFieldsToMask: ['FIELD_TO_MASK_1'],
    },
  },
}
```

> Do replace `FIELDS_TO_MASK_1` with the field you want to mask.

### routesToMonitor

By default, this Strapi plugin will monitor only routes that starts has `/api` in the URL. You can override this by passing a `routesToMonitor` array in the `config` of the plugin.

```js
module.exports = {
  treblle: {
    config: {
      routesToMonitor: ['api'], // this is the default
    },
  },
}
```